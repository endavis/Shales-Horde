ext {
    libs = [
            // Minecraft and Fabric
            minecraft             : "com.mojang:minecraft:$project.minecraft_version",
            yarn                  : "net.fabricmc:yarn:$project.minecraft_version+build.$project.yarn_mappings:v2",
            fabric_loader         : "net.fabricmc:fabric-loader:$project.fabric_loader",

            // Hard Dependencies
            fabric_api            : "net.fabricmc.fabric-api:fabric-api:$project.fabric_version",
    ]
}